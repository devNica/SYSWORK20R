import { persons_model } from './persons';
import {positions_model} from './positions';
import {locations_model} from './locations';
import {employees_model} from './employees'

export const models = {
    persons: persons_model,
    positions: positions_model,
    locations: locations_model,
    employees: employees_model,
}